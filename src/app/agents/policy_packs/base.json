{
  "id": "base_policy_v1",
  "match": {
    "source_servers": ["*"]
  },
  "inject": {
    "append_system_message": [
      "RELIABILITY:",
      "- If a tool returns JSON with error_type=validation_error, fix args to match args_schema and retry once.",
      "- Never repeat the same invalid args.",
      "TASK INSTRUCTIONS:",
      "- If [TASK_INSTRUCTIONS] specifies tool calls, you MUST execute them before answering.",
      "- If the tool is unavailable, say so clearly (do not fabricate results)."
    ]
  }
}

